# Production settings.
# Secrets (secret_key, google_client_secret) should come from
# environment variables (FAROS_SECRET_KEY, etc.), not this file.

database_url: "sqlite+aiosqlite:///faros.db"
token_expire_minutes: 60
base_url: "https://faros.example.com"

google_auth_url: "https://accounts.google.com/o/oauth2/v2/auth"
google_token_url: "https://oauth2.googleapis.com/token"
google_userinfo_url: "https://www.googleapis.com/oauth2/v2/userinfo"
